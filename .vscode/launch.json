{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "cortex-debug",
            "cwd": "${workspaceRoot}",
            "executable": "${folder}/main.elf",
            "request": "launch",
            "type": "cortex-debug",
            "servertype": "openocd",
            "configFiles": [
                "board/stm32f0discovery.cfg"
            ],
            "device": "STM32F072RB",
            "svdFile": "${workspaceRoot}/STM32F072x.svd"
        }
    ]
}